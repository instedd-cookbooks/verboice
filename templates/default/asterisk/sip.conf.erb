[general]
srvlookup=yes
externip=<%= @params[:external_ip] %>
<% @params[:local_networks].each do |net| %>
localnet=<%= net %>
<% end %>
disallow=all
allow=gsm
allow=ulaw
allow=alaw
;allow=g729
dtmfmode=auto
#include sip_verboice_registrations.conf

#include sip_verboice_channels.conf
